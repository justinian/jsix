# vim: ft=python

module("testapp",
    targets = [ "user" ],
    deps = [ "libc" ],
    description = "Testbed app",
    sources = [
        "io.cpp",
        "main.cpp",
        "serial.cpp",
    ])
