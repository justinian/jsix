# vim: ft=python

module("drv.uefi_fb",
    targets = [ "user" ],
    deps = [ "libc" ],
    description = "UEFI framebuffer driver",
    sources = [
        "font.cpp",
        "main.cpp",
        "screen.cpp",
        "scrollback.cpp",
    ])

