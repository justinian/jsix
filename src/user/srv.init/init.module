# vim: ft=python

init = module("srv.init",
    targets = [ "user" ],
    deps = [ "libc", "elf", "bootproto", "zstd" ],
    description = "Init server",
    sources = [
        "loader.cpp",
        "main.cpp",
        "modules.cpp",
        "ramdisk.cpp",
        "service_locator.cpp",
        "start.s",
    ])
