# vim: ft=python

init = module("srv.init",
    targets = [ "user" ],
    deps = [ "libc", "elf", "bootproto", "zstd" ],
    description = "Init server",
    ld_script = "init.ld",
    sources = [
        "j6romfs.cpp",
        "loader.cpp",
        "main.cpp",
        "modules.cpp",
        "service_locator.cpp",
        "start.s",
    ])
