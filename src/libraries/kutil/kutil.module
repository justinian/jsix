# vim: ft=python

module("kutil",
    kind = "lib",
    includes = [ "include" ],
    sources = [
        "assert.cpp",
        "bip_buffer.cpp",
        "heap_allocator.cpp",
        "memory.cpp",
        "spinlock.cpp",
    ])
