LOG(apic,   info);
LOG(device, debug);
LOG(paging, info);
LOG(driver, info);
LOG(memory, debug);
LOG(fs,     info);
LOG(task,   info);
LOG(sched,  info);
LOG(loader, debug);
LOG(boot,   debug);
LOG(syscall,debug);
LOG(vmem,   debug);
LOG(objs,   debug);
LOG(timer,  debug);
LOG(clock,  debug);
