LOG(apic,   info);
LOG(device, debug);
LOG(paging, warn);
LOG(driver, info);
LOG(memory, info);
LOG(fs,     info);
LOG(task,   debug);
LOG(loader, info);
LOG(boot,   debug);
LOG(syscall,debug);
LOG(vmem,   debug);
LOG(objs,   debug);
LOG(timer,  debug);
LOG(clock,  debug);
