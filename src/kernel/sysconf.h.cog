#pragma once
// vim: ft=cpp

/// \file sysconf.h
/// Kernel-side implementation of sysconf struct

/*[[[cog code generation
from pathlib import Path
from jsix.sysconf import load_sysconf
address, vars = load_sysconf(Path(definitions_path) / "sysconf.toml")

cog.outl(f"constexpr uintptr_t sysconf_user_address = {address:#x};")
]]]*/
///[[[end]]]

struct system_config
{
    /*[[[cog code generation
    for var in vars:
        cog.outl(f"{var.type:10} {var.section}_{var.name};")
    ]]]*/
    ///[[[end]]]
};

extern system_config *g_sysconf;
extern uintptr_t g_sysconf_phys;

void sysconf_create();
